package model

/** Clase que representa un experimento de Toy, extiende de DataExperiment.
 *
 *  @param l Lista de cadenas que representa los datos del experimento.
 */
class ToyExperiment(l: List[String]) extends DataExperiment(l) {
  // Aquí podrías agregar métodos específicos para manipular los datos del experimento de juguete si es necesario
}
