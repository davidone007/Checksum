package model

/**
 * Clase que representa un experimento de categoría Grande.
 *
 * @param l Lista de cadenas que representan los datos del experimento.
 */
class GrandeExperiment(l: List[String]) extends DataExperiment(l) {
}
