package model

/** Clase que representa un experimento Pequena, extiende de DataExperiment.
 *
 *  @param l Lista de cadenas que representa los datos del experimento.
 */
class PequenaExperiment(l: List[String]) extends DataExperiment(l) {
  // Aquí podrías agregar métodos específicos para manipular los datos del experimento pequeño si es necesario
}
